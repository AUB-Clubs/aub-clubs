apiVersion: v1
kind: Service
metadata:
  name: inference
  namespace: aub-clubs
spec:
  type: ClusterIP
  selector:
    app: inference
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
